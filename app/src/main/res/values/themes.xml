<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de arranque (androidx.core.splashscreen): el ícono se ve sobre un
         chip morado de marca (windowSplashScreenIconBackgroundColor) para que
         el glifo blanco lea igual en claro y oscuro, aunque el fondo de la
         pantalla completa siga el token Background/BackgroundDark del theme. -->
    <style name="Theme.Equilibrio.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/brand_purple</item>
        <item name="postSplashScreenTheme">@android:style/Theme.Material.Light.NoActionBar</item>
    </style>
</resources>
